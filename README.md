# dotfiles

My Rice Dotfiles [Fedora + Hyprland]

## Installation

**1. Run "enable_multilib.sh" script as root.**

**2. Run "install.sh" script as root.**

**3. Run "install_aur_yay_apps.sh" script as root.**

**4. Run "install_flathub_apps.sh" script as root.**
